import { fetchTags, createTag, deleteTag } from "./tags";

export const tagsApi = { fetchTags, createTag, deleteTag };
export {
  fetchCards,
  createCard,
  updateCard,
  deleteCard,
  setCardLearned
} from "./cards";
